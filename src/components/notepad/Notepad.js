import React from 'react';
import './Notepad.css';

const Notepad = () => {
    return (
        <div>
            <textarea className="notepad" rows={35} />
        </div>
    )
}

export default Notepad